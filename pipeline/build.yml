---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

inputs:
  - name: source
  - name: version

outputs:
  - name: dist

caches:
  - path: source/pipeline/.gradle

run:
  path: source/pipeline/build.sh
